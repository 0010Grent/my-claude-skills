# arxiv-paper-reporter 依赖清单
# 安装：pip install -r requirements.txt

# PDF 解析（二选一，推荐 pymupdf）
pymupdf>=1.23.0          # 快速、高质量的 PDF 解析（PyMuPDF / fitz）
# pdfminer.six>=20221105 # 备选方案

# HTTP 请求（arxiv API 获取）
# 标准库 urllib 已内置，无需额外安装
requests>=2.31.0         # 可选，用于更复杂的 HTTP 场景

# 数据处理
dataclasses-json>=0.6.4  # dataclass JSON 序列化

# 开发与测试（可选）
pytest>=7.4.0
black>=23.0.0
